# Redis has been removed from this project.
# This file is intentionally empty — kept so any stale bytecode imports fail loudly.
raise ImportError(
    "app.redis has been removed. Do not import from it. "
    "Remove all 'from app.redis import ...' statements."
)
